# Commit b1db6d5 — 24/04/2026

---

## 1. Arquivos do Residente — redesign para lista moderna + edição inline

### `src/components/Arquivos/Arquivos_TableView.tsx`

Reescrita completa do componente, de tabela simples para lista moderna.

- **Badge de formato**: `FORMAT_CONFIG` com cores distintas para PDF (vermelho), IMG (rosa), DOC (azul), XLS (verde), FILE (cinza)
- **Edição inline**: ao clicar no lápis, a linha vira formulário com input de descrição + chips de categoria; botões Salvar (✓) e Cancelar (✗) sempre visíveis no modo edição
- **Estado de edição**: `Record<string, EditState>` keyed por `_id`; `saving` controla o disabled do botão
- **Ações hover-reveal**: editar, baixar e excluir ocultos até hover via `group/group-hover:opacity-100`; excluir visível apenas para admins
- **Metadados com ícones**: `FaCalendarAlt` para data de upload e `FaUserCircle` para quem enviou; ambos com `title` (tooltip) explicativo
- **Exibição de descrição**: mostra `descricao` (quando diferente do filename) acima do nome do arquivo
- **Nova prop `onRefresh`**: chamada após salvar edição para recarregar a lista no pai

### `src/components/Arquivos/GestaoArquivos.tsx`

- Passa `onRefresh={() => setTriggerRefresh(prev => !prev)}` para `Arquivos_TableView`

---

## 2. Arquivos — controller com edição e lookup de nomes

### `src/pages/api/Controller/ArquivosAdmin.ctrl.tsx`

Refatorado para suportar GET e PUT.

**GET (ajustes)**:
- Corrigido fallback de `descricao`: era `doc.descricao || categoria || doc.originalName` (mostrava a categoria como descrição quando vazio); agora `doc.descricao || doc.originalName`
- Lookup de nomes: agrega `createdBy` únicos → consulta `usuario` collection com `$in` (uma query só, não N+1) → retorna `fullName` como nome completo

**PUT (novo)**:
- Lê o documento antes (`antes`) para comparar
- Atualiza `descricao`, `tags` (categoria), `updatedAt`, `updatedBy`
- Registra auditoria separada para cada campo alterado via `registrarAuditoria(db, { entidade: 'arquivosr2', acao: 'editar_descricao' | 'editar_categoria', ... })`

---

## 3. Suprimentos — histórico por item em modal

### `src/components/Residentes/Suprimentos.tsx`

- **Linhas da tabela clicáveis**: `onClick` em cada `<tr>` abre modal com histórico filtrado para aquele insumo
- **Botões +/− com `stopPropagation`**: evitam acionar o modal ao clicar nas ações
- **Estado do modal**: `historicoInsumo`, `modalHistorico`, `modalCount`, `modalPage`, `modalLoading` separados do histórico geral
- **`loadModalHistorico`**: função dedicada que chama `getHistoricoPaginado` com `insumo_id` opcional; suporta paginação incremental (append)
- **Modal**: slide-up no mobile (items-end), centralizado no desktop; cabeçalho com nome do insumo; `HistoricoSuprimentos` reutilizado com "Carregar mais" próprio; fecha no backdrop ou ×
- Histórico geral abaixo da tabela permanece inalterado

### `src/pages/api/Controller/InsumoEstoque.tsx`

- `getHistoricoPaginado`: aceita `insumo_id` opcional → adiciona ao `matchStage` antes do aggregate e do `countDocuments`; a contagem do "carregar mais" fica correta para o item filtrado

---

## 4. Página de detalhes do idoso — redesign de perfil

### `src/pages/portal/administrativo/idosos/[id]/index.tsx`

- **Removido banner/cover**: eliminado o `<div>` de gradiente (`from-slate-700 via-indigo-800 to-purple-900`); header agora é plano
- **Layout do header**: `flex-col items-center` no mobile (avatar centralizado, texto centralizado), `flex-row` no desktop
- **Avatar**: removido wrapper `ring-4` externo que ficava aplicado num `<div>` não-circular (o `AvatarCropper` já tem `border-4 border-white shadow-md` internamente); fallback e `<Image>` recebem a mesma borda
- **Tab bar no mobile**: labels ocultados com `hidden sm:inline`; exibe só ícones (ligeiramente maiores: `text-sm`); `title` com o nome da aba serve como tooltip; evita 3+ linhas de abas em telas pequenas
- **Subtítulo do header**: exibe `"apelido" · DD/MM/YYYY (N anos)` em vez de modalidade + prontuário + data de entrada
- **`calcIdade(dateStr)`**: helper que calcula idade considerando dia/mês exatos

### `src/pages/api/Controller/C_idosoDetalhes.ts`

- Adicionado `apelido: { $ifNull: ['$_u.apelido', null] }` ao `$addFields` do pipeline `lookupUsuarioPipeline`

### `src/types/T_idosoDetalhes.ts`

- `T_IdosoDetalhesComUsuario.usuario`: adicionado campo `apelido?: string`
